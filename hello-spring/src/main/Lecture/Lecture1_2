환경 설정 - 라이브러리 살펴보기
    gradle이나 maven은 서로 의존 관계에 있는 라이브러리들을 모두 땡겨온 다음에 관리를 해준다.

    이렇게 서버를 내장하고있는것을 임베디드라고 합니다.

    라이브러리를 빌드해가지고 그걸 웹서버에 올리면 끝이다.

    print()로 찍으면 안되고 항상 로그라는것으로 출력해야 한다!!
        왜? 로그로 남겨야 에러를 모아볼 수 있고, 로그를 관리할 수 있다.
        로그에 대해서 알아보고 싶다면 slf4j, logback 학습, 요 두가지로 로그관리가 요즘 표준
