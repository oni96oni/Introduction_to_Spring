스프링과 빈의 의존관계 - 자바 코드로 직접 스프링 빈 등록하기
    @Controller는 어떻게 해줄 수가 없다 그냥 등록하고 사용

    springconfig하는것이 수동

    di주입에는 3가지 방법이있다.
    1. setter 주입
    2. constructor 주입
    3. 의존성 주입

    의존성 주입은 의존성을 가진 클래스를 스프링에서 자동으로 주입해준다.

    필드주입은 선호되지 않는다. 처음에 띄울때 빼고는 바꿀수 있는 방법이 없어서
    
    set은 public으로 노출되어 있어서 중간에 바꾸면 문제가 발생할 수 있는 여지가 있다.

    생성자주입이 선호 의존관계가 실행중에 동적으로 변하는 경우는 없으므로!
        동적으로 변한다는것은 runtime때 뭔가가 바뀐다는것 이런경우 에는 config파일을 바꾼다.



