스프링 DB 접근 기술 - 스프링 데이터 jpa

    JpaRepository를 구현하면
    구현체를 알아서 만들어서 스프링빈에다가 등록을 해놓는다. 그러면 우리는 자연스럽게 가져다 쓰기만 하면된다.
    스프링 데이터 JPA가 SpringDataJpaMemberRepository 를 스프링 빈으로 자동 등록해준다.
    공통화할 수 있는것들은 다 만들어서 준다.
    단순한것은 인터페이스만으로도 만들 수 있고 복잡한것만 우리가 만들면 된다!

