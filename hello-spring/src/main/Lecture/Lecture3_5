회원 관리 예제 - 백엔드 개발 - 회원 서비스 테스트
    테스트할때는 메서드이름 과감하게 한글로해도 상관없다! 빌드될때 실제코드에 포함되지 않는다.

    given / when / then
    주어졌을때
    실행했을때
    결과가 이것이 나와야 한다.

    try-catch말고 다른 문법 또한 제공
    assertThrows()

    static은 인스턴스와 상관없이 클래스 레벨에 붙는것

    여러 클래스에서 서로 생성을 하면 다른 클래스! 이를 위해서 di적용 외부에서 넣어주는 방식
    직접 new 생성이 아니라!